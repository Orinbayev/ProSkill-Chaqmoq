# core/middleware_prod.py
# ────────────────────────────────────────────────────────────────
# DEPRECATED — this file is no longer used.
# The active middleware is core.middleware.TenantMiddleware
# which uses session-based center resolution (NOT subdomains).
#
# Kept only to avoid import errors in case any old config references it.
# Safe to delete once confirmed nothing imports this file.
# ────────────────────────────────────────────────────────────────
